{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    
    
    <h1>Acteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ actor.name }}</td>
            </tr>
        </tbody>
    </table>
     	<div class="d-flex justify-content-between  align-items-center">
    <a class="btn btn-outline-secondary my-3 form-btn"  href="{{ path('actor_app_actor_index') }}">Retour</a>

    <a class="btn btn-outline-light my-3 form-btn"  href="{{ path('actor_app_actor_edit', {'id': actor.id}) }}">Editer</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
