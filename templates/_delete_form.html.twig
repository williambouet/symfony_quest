<form method="post" action="{{ path('actor_app_actor_delete', {'id': actor.id}) }}" onsubmit="return confirm('Veuillez confirmer la demande de suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
    <button class="btn btn-outline-danger self-align-end form-btn">Supprimer</button>
</form>
