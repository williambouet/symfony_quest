{% extends 'base.html.twig' %}

{% block title %}Edit Season{% endblock %}

{% block body %}
    <h1>Modifier une saison</h1>
	{{ include('_bs_form/_bs_top_form.html.twig')}}
    {{ include('season/_form.html.twig', {'button_label': 'Editer'}) }}

    {{ include('season/_delete_form.html.twig') }}
	</div>
		<a class="btn btn-outline-secondary my-3" href="{{ path('app_season_index') }}">Retour</a>

    	{{ include('_bs_form/_bs_bottom_form.html.twig')}}
{% endblock %}
